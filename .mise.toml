[tools]
bats = "1"
editorconfig-checker = "3"
"pipx:pre-commit" = { version = "4", uvx_args = "--with pre-commit-uv" }
shellcheck = "0.11"
shfmt = "3"
uv = "0.10.0"

[settings]
idiomatic_version_file_enable_tools = [
]

[task_config]
includes = [
    "toolbox/scripts",
]
